module names

imports include/QL
	
namespaces Frm Quest

rules

	Form(n,_):
		defines Frm n
		scopes Quest
	
	Question(q, _, ty):
		defines Quest q of type ty in subsequent scope

	Computed(q, _, TExpr(ty, _)):
		defines Quest q of type ty in subsequent scope
	
	Ref(q):
		refers to Quest q

